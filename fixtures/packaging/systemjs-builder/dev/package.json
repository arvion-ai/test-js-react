{
  "private": true,
  "name": "systemjs-dev-fixture",
  "dependencies": {
    "systemjs-builder": "^0.15.34"
  },
  "scripts": {
    "build": "rm -f output.js && node build.js"
  },
  "overrides": {
    "semver": "7.7.3"
  }
}
